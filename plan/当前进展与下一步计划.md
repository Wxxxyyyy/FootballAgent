# 📋 当前进展与下一步计划

> 更新时间：2026-06-24 19:33
> 基于实际代码完成情况编写，非理论规划

---

## 一、已完成清单

### ✅ ML 模型（已完成）

| 项目 | 状态 | 位置 |
|------|------|------|
| 特征工程 19维 | ✅ | `agents/predicted_agent/feature_engineering.py` |
| LightGBM 训练（V5） | ✅ 验证集54.51%，世界杯56.25% | `agents/predicted_agent/models/statistical_model.py` |
| 10版实验对比 | ✅ | `agents/predicted_agent/models/saved/model_experiments.md` |
| 蒙特卡洛模拟器 | ✅ 无需训练，赔率→λ→泊松模拟 | `agents/predicted_agent/models/monte_carlo_simulator.py` |
| 2022世界杯测试集 | ✅ 64场，初盘+终盘+比分 | `agents/predicted_agent/models/saved/wc2022_test.csv` |

### ✅ 赛前情报模块（已完成）

| 项目 | 状态 | 位置 |
|------|------|------|
| 伤停爬虫（懂球帝API） | ✅ | `agents/predicted_agent/scouters/injury_suspension_scouter.py` |
| 赛前新闻爬虫（懂球帝API） | ✅ | `agents/predicted_agent/scouters/news_scouter.py` |
| 首发两档（赛前>1h缺阵预判 / ≤1h官方首发） | ✅ | `agents/predicted_agent/scouters/lineup_scouter.py` |
| 教练风格+惯用阵型 | ✅ 32队静态数据 | `agents/predicted_agent/scouters/coach_style_scouter.py` |
| 国家队映射（48队中英文/别名） | ✅ | `agents/predicted_agent/scouters/national_team_config.py` |
| 情报聚合器 | ✅ | `agents/predicted_agent/scouters/pre_match_intel.py` |

### ✅ 赔率数据源（已完成）

| 项目 | 状态 | 位置 |
|------|------|------|
| titan007 实时赔率爬取 | ✅ 1x2d.titan007.com/{id}.js | 验证通过 |
| 赔率快照管理（初盘+终盘） | ✅ 首次记录初盘，后续更新终盘 | `pipeline/odds_snapshot_manager.py` |
| 2022世界杯历史赔率爬虫 | ✅ 64场全部爬取 | `agents/predicted_agent/scripts/wc2022_odds_scraper.py` |
| football-data.co.uk 联赛数据 | ✅ 训练集+验证集 | `data/processed/*.csv` |

### ✅ 预测流程与调度（已完成）

| 项目 | 状态 | 位置 |
|------|------|------|
| 7步预测流水线 | ✅ ML+蒙特卡洛+交锋+情报+爆冷+LLM | `agents/predicted_agent/advance_predictor.py` |
| LLM 预测器（去掉大小球，加蒙特卡洛） | ✅ | `agents/predicted_agent/models/llm_predictor.py` |
| 爆冷信号分析 6维（含伤员预警） | ✅ | `advance_predictor.py` `_analyze_upset_signals` |
| 预测调度模块 | ✅ 每小时检查，赛前24h/1h触发 | `pipeline/prediction_scheduler.py` |
| 调度器接入 | ✅ 已接入 APScheduler | `pipeline/scheduler.py` |
| 预测结果存储 | ✅ JSON 文件 | `data/predictions/` |

### ✅ 文档（已完成）

| 项目 | 状态 | 位置 |
|------|------|------|
| 预测流水线规范 | ✅ 架构图+数据流+LLM格式+Agent Loop | `docs/prediction_pipeline_spec.md` |
| 模型实验记录 | ✅ 10版对比 | `agents/predicted_agent/models/saved/model_experiments.md` |

---

## 二、未完成 / 待做（按优先级排序）

### 🔴 Step 1: 端到端测试 — 验证完整预测链路能跑通

**问题**：虽然各模块都实现了，但从来没有跑过一次完整的预测流程。需要验证 7 步流水线是否能端到端跑通。

**怎么做**：
```bash
# 手动触发一场比赛预测（用今晚的比赛）
python -m pipeline.prediction_scheduler 瑞士 加拿大 2026-06-25 1
```

**预期可能的问题**：
- OpenClaw 容器可能没运行 → `_request_openclaw()` 超时
- Neo4j 可能没有国家队交锋数据 → `_query_h2h()` 返回空
- 懂球帝 API 可能返回空 → 情报采集降级
- LLM API key 可能没配 → LLM 分析失败

**验证标准**：能输出一个完整的 JSON 预测结果文件到 `data/predictions/`

---

### 🔴 Step 2: OpenClaw Docker 容器对接确认

**问题**：`advance_predictor.py` 的 `_request_openclaw()` 向 OpenClaw 容器发 HTTP 请求获取赔率+近5场。需要确认：
1. OpenClaw 容器是否在运行？
2. OpenClaw 返回的数据格式是否和代码期望的一致？
3. 如果 OpenClaw 不可用，能否用赔率快照管理器直接替代？

**怎么做**：
```bash
# 检查 OpenClaw 容器状态
docker ps | grep openclaw

# 测试 OpenClaw API
curl http://localhost:9000/health
curl -X POST http://localhost:9000/task -H "Content-Type: application/json" \
  -d '{"task_type":"pre_match_analysis","params":{"home_team":"瑞士","away_team":"加拿大"}}'
```

**如果 OpenClaw 不可用**：
- 修改 `advance_predictor.py`，当 OpenClaw 失败时降级为从赔率快照读取赔率
- 近5场数据从 titan007 或 football-data 历史数据中获取

---

### 🟡 Step 3: Neo4j 国家队交锋数据确认

**问题**：现有 Neo4j 里可能是俱乐部数据（五大联赛），世界杯预测需要国家队交锋数据。

**怎么做**：
```bash
# 检查 Neo4j 中是否有国家队数据
# 在 Neo4j Browser 中执行:
MATCH (t:Team) RETURN DISTINCT t.name LIMIT 20
```

**如果没有国家队数据**：
- 选项A：从 football-data.co.uk 的 WorldCup 历史数据导入国家队交锋记录
- 选项B：跳过 Neo4j，用赔率快照的历史数据替代
- 选项C：用 titan007 爬取历史交锋

---

### 🟡 Step 4: 启动定时调度，实战验证

**问题**：调度代码写好了但没实际跑过。

**怎么做**：
```bash
# 启动调度器
python -c "from pipeline.scheduler import start_scheduler; import time; start_scheduler(); time.sleep(99999)"
```

**验证标准**：
1. 每小时自动获取比赛列表 ✅
2. 每小时自动更新赔率快照 ✅
3. 赛前24h自动触发第一档预测 ✅
4. 赛前1h自动触发第二档预测 ✅
5. 预测结果保存到 `data/predictions/` ✅

---

### 🟡 Step 5: 蒙特卡洛与 LLM 整合验证

**问题**：蒙特卡洛模拟已实现，LLM prompt 已更新加入蒙特卡洛段落，但还没验证 LLM 能否正确利用蒙特卡洛的比分分布。

**怎么做**：
- 跑一次完整预测，检查 LLM 输出的 `score_predictions` 是否和蒙特卡洛的 `score_distribution` 一致或接近
- 如果 LLM 完全忽略蒙特卡洛数据，需要在 prompt 中加强引导

---

### 🟢 Step 6: 预测结果展示（可选）

**问题**：目前预测结果只存 JSON 文件，不方便查看。

**怎么做**（三选一）：
- **简单版**：写一个脚本把 JSON 格式化为可读的文本报告
- **API版**：加一个 FastAPI 端点 `/predictions/{match_id}` 返回预测结果
- **前端版**：简单 HTML 页面展示预测结果

---

### 🟢 Step 7: 赛后验证 — 预测准确率跟踪

**问题**：预测做完后需要和实际结果对比，验证效果。

**怎么做**：
1. 比赛结束后，从 titan007 获取实际比分
2. 和预测结果对比，记录准确率
3. 累积多场后统计整体准确率

---

## 三、建议执行顺序

```
Step 1: 端到端测试（今天）
  └── 发现问题 → 修复
      ├── Step 2: OpenClaw 对接（如果 OpenClaw 不可用）
      └── Step 3: Neo4j 数据（如果没有国家队数据）
  └── Step 4: 启动定时调度（验证通过后）
      └── Step 5: 蒙特卡洛+LLM 验证（实战中检查）
          └── Step 6: 结果展示（有余力再做）
              └── Step 7: 赛后验证（持续跟踪）
```

**今天最应该做的**：Step 1 端到端测试，用今晚的比赛（瑞士 vs 加拿大，03:00开赛）跑一次完整预测，看哪里报错就修哪里。

---

## 四、关键文件索引

| 文件 | 作用 |
|------|------|
| `agents/predicted_agent/advance_predictor.py` | 7步预测主流程 |
| `agents/predicted_agent/feature_engineering.py` | 19维特征工程 |
| `agents/predicted_agent/models/statistical_model.py` | LightGBM 训练+预测 |
| `agents/predicted_agent/models/monte_carlo_simulator.py` | 蒙特卡洛模拟 |
| `agents/predicted_agent/models/llm_predictor.py` | Kimi LLM 综合分析 |
| `agents/predicted_agent/scouters/` | 赛前情报模块 |
| `pipeline/odds_snapshot_manager.py` | 赔率快照管理 |
| `pipeline/prediction_scheduler.py` | 预测调度 |
| `pipeline/scheduler.py` | APScheduler 定时任务 |
| `docs/prediction_pipeline_spec.md` | 预测流水线规范 |
