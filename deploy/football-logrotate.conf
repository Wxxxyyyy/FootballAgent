# FootballAgent 日志轮转（安装到 /etc/logrotate.d/football-agent）
# 预测服务/准确率跟踪日志由 systemd append 写入，会无限增长，按周轮转保留 8 份。
/data/workspace/FootballAgent/logs/*.log {
    weekly
    rotate 8
    maxsize 50M
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
