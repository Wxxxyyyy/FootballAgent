# ⚽ Football Agent - Redis 配置
# 用于 Docker 或本地 Redis 启动

# 监听端口
port 6379

# 持久化 - AOF 模式
appendonly yes
appendfsync everysec

# 最大内存限制 (256MB)
maxmemory 256mb
maxmemory-policy allkeys-lru

# 日志级别
loglevel notice

